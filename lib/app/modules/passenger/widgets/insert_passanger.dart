import 'package:flutter/material.dart';

class InsertPassanger extends StatelessWidget {

  const InsertPassanger({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(title: Text('teste'),),
         body: Container(
           color: Colors.black,
         ),
       );
  }
}